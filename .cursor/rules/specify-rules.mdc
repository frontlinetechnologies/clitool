# clitool Development Guidelines

Auto-generated from all feature plans. Last updated: 2025-12-16

## Active Technologies
- TypeScript 5.x with strict mode (Node.js LTS) + @anthropic-ai/sdk (Anthropic API client), @playwright/test (Playwright test framework), commander (CLI), existing crawl models and documentation modules (003-generate-tests)
- In-memory during processing, output to filesystem (no persistent storage required) (003-generate-tests)

- TypeScript 5.x with strict mode (Node.js LTS) + Playwright, cheerio (HTML parsing), commander (CLI), robots-parser (robots.txt parsing) (001-web-crawl)
- TypeScript 5.x with strict mode (Node.js LTS) + @anthropic-ai/sdk (Anthropic API client), commander (CLI) (002-generate-docs)

## Project Structure

```text
src/
tests/
```

## Commands

npm test && npm run lint

## Code Style

TypeScript 5.x with strict mode (Node.js LTS): Follow standard conventions

## Recent Changes
- 003-generate-tests: Added TypeScript 5.x with strict mode (Node.js LTS) + @anthropic-ai/sdk (Anthropic API client), @playwright/test (Playwright test framework), commander (CLI), existing crawl models and documentation modules

- 001-web-crawl: Added TypeScript 5.x with strict mode (Node.js LTS) + Playwright, cheerio (HTML parsing), commander (CLI), robots-parser (robots.txt parsing)
- 002-generate-docs: Added TypeScript 5.x with strict mode (Node.js LTS) + @anthropic-ai/sdk (Anthropic API client), commander (CLI)

<!-- MANUAL ADDITIONS START -->
<!-- MANUAL ADDITIONS END -->
